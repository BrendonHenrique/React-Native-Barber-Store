type AccountType = {
  /**
   * @param nome
   * account user name
   */
  nome?: string;
  /**
   * @param email
   * account user email
   */
  email: string;
  /**
   * @param password
   * account user password
   */
  password: string;
};

export default AccountType;
