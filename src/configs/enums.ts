export enum RegisterService {
  SUCCESS = "Conta criada com sucesso",
  ERROR = "Falha ao criar a conta",
}

export enum LoginService {
  SUCCESS = "Conta encontrada com sucesso",
  ERROR = "Falha ao encontrar a conta",
}

export enum Collections {
  ACCOUNTS = "accounts",
}
